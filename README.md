Primitive agent

Boot.dev project

usage: at CLI, type: uv run main.py "Your prompt here"
The app's Working directory is limited to the 'calculator' app in the subdirectory. It is designed to use AI to debug and fix that specific app.


requires a .env file containing:
GEMINI_API_KEY=YOUR_KEY_HERE